<section id="homepage">
  {{#each posts as |post|}}
    <article>
      <h2>{{post.title}}</h2>
      <p>{{post.summary}}</p>
      <span>Posted by {{post.user.name}} on {{format_date post.createdAt}}</span>
    </article>
  {{/each}}
</section>